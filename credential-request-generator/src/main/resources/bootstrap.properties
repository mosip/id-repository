spring.cloud.config.uri=https://qa.mosip.net/config/
spring.cloud.config.label=master
spring.cloud.config.name=application
spring.application.name=application
spring.profiles.active=mz

#management.security.enabled=false
#management.endpoint.health.show-details=when-authorized
#management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=info,health,refresh,restart


server.port=8094
server.servlet.path=/credentialrequest/v1

#disabling health check so that client doesnt try to load properties from sprint config server every
# 5 minutes (should not be done in production)
health.config.enabled=false
# added for testing purpose
# Database connection URL
javax.persistence.jdbc.url=jdbc:h2:mem:testdb?currentSchema=creden

#DB user name
javax.persistence.jdbc.user=sa

#DB password
javax.persistence.jdbc.password=
hibernate.dialect=org.hibernate.dialect.H2Dialect

javax.persistence.jdbc.driver=org.h2.Driver