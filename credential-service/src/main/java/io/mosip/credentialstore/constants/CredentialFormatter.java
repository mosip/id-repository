package io.mosip.credentialstore.constants;

/**
 * The Enum CredentialFormatter.
 * 
 * @author Sowmya
 */
public enum CredentialFormatter {

	/** The id auth provider. */
	idAuthProvider,
	/** The credential default provider. */
	credentialDefaultProvider,
	/** The pin based provider. */
	pinBasedProvider;
}
