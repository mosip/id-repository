spring.cloud.config.uri=localhost
spring.cloud.config.label=master
spring.profiles.active=dev
spring.cloud.config.name=id-repository,application,kernel
spring.application.name=id-repository,application

#management.security.enabled=false
#management.endpoint.health.show-details=when-authorized
#management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=info,health,refresh,restart

server.port=8092

#disabling health check so that client doesnt try to load properties from sprint config server every
# 5 minutes (should not be done in production)
health.config.enabled=false

spring.jpa.database-platform=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Enable SQL Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Primary Database
datasource.primary.url=jdbc:postgresql://dev0.mosip.net:5433/mosip_idmap
datasource.primary.username=postgres
datasource.primary.password=PI7vp1Q1Sp
datasource.primary.driver-class-name=org.postgresql.Driver

# Identity Database
datasource.identity.url=jdbc:postgresql://dev0.mosip.net:5433/mosip_idmap
datasource.identity.username=postgres
datasource.identity.password=PI7vp1Q1Sp
datasource.identity.driver-class-name=org.postgresql.Driver

# VID Database
datasource.vid.url=jdbc:postgresql://dev0.mosip.net:5433/mosip_idrepo
datasource.vid.username=postgres
datasource.vid.password=PI7vp1Q1Sp
datasource.vid.driver-class-name=org.postgresql.Driver
